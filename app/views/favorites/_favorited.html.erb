  <% if @training.favorited_by?(current_user) %>
    <p>
      <%= link_to post_image_favorites_path(training), method: :delete do %>
        ♥<%= training.favorites.count %> いいね
      <% end %>
    </p>
    <% else %>
    <p>
      <%= link_to post_image_favorites_path(training), method: :post do %>
        ♡<%= training.favorites.count %> いいね
      <% end %>
    </p>
  <% end %>